# ExpenseTracker.Api

Backend for Smart Expense Tracker – Phase 1 (API + Infrastructure).

To run navigate to root and and CLI run "Docker Compose Up" 

This will run migrations and create database within a contianer. 

There are postman collection .json's in a folder in the root folder which you can import to run against the api. 
