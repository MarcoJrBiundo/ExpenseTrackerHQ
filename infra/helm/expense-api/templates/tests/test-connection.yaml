apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "expense-api.fullname" . }}-test-connection"
  labels:
    {{- include "expense-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "expense-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
